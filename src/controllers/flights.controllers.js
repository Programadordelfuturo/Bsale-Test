const getFlight = async (req, res) => {
  // try {
  //   // const { id } = req.params
  //   // const result = await .findByPk(id);
  //   res.json('jajaja')
  // } catch (error) {
  //   res.json(error.message);
  // }
  res.send('go')
}

module.exports = {
  getFlight,
}